<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ActionVideo.Converts"
             x:Class="ActionVideo.Views.HomePage">
    <ContentPage.Resources>
        <ResourceDictionary>
            <local:ImageSourceConverter x:Key="Pic"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <Shell.SearchHandler>
        <SearchHandler Placeholder="请输入关键字"
                                      ShowsResults="true"
                                      DisplayMemberName="Name" />
    </Shell.SearchHandler>
    <CollectionView x:Name="Videos" ItemsLayout="VerticalGrid,2" BackgroundColor="{StaticResource Primary}">
        <CollectionView.ItemTemplate>
            <DataTemplate>
                <StackLayout Padding="2">
                    <Image Source="{Binding Pic,Converter={StaticResource Pic}}" Aspect="AspectFill"/>
                    <Label Text="{Binding Name}" MaxLines="3"/>
                    <Label Text="{Binding DateTime}" HorizontalOptions="Start"/>
                </StackLayout>
            </DataTemplate>
        </CollectionView.ItemTemplate>
    </CollectionView>
</ContentPage>
